{
  "name": "personal-website-react",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "NEXT_PUBLIC_BASE_PATH=/personal-website-react next build",
    "export": "NEXT_PUBLIC_BASE_PATH=/personal-website-react next build",
    "deploy": "NEXT_PUBLIC_BASE_PATH=/personal-website-react next build && touch out/.nojekyll && npx gh-pages -d out --dotfiles",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "@emailjs/browser": "^4.4.1",
    "next": "^15.5.4",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
