{
  "name": "personal-website-react",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "export": "next build",
    "deploy": "next build && touch out/.nojekyll && npx gh-pages -d out --dotfiles",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "^15.5.4",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
